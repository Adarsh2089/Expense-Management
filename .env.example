# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=expense_management
DB_USERNAME=postgres
DB_PASSWORD=postgres

# JWT Configuration
JWT_SECRET=your-very-long-secret-key-change-this-in-production-min-256-bits
JWT_EXPIRATION_MS=86400000

# Application Configuration
SERVER_PORT=8080

# External API Configuration (Optional - for offline/mock fallback)
ENABLE_EXTERNAL_API=true
COUNTRIES_API_URL=https://restcountries.com/v3.1/all?fields=name,currencies
CURRENCY_RATES_API_URL=https://api.exchangerate-api.com/v4/latest

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4200
